<h1>Class Page</h1>

<% @classes.each do |c| %>
<li>
    <%= link_to c.title, school_class_path(c) %>
</li>

<% end %>
<p><%= link_to 'New Class', new_school_class_path%></p>
<p><%= link_to 'Home', home_path %></p>
