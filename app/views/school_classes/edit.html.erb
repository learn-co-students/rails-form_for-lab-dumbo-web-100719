<h2>Editing <%= @school_class.title %> </h2>

<%= form_for @school_class do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :room_number%>
  <%= f.number_field :room_number %>

  <%= f.submit %>
<% end %>